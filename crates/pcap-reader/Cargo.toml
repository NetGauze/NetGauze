[package]
name = "netgauze-pcap-reader"
version = "0.1.0"
edition = "2021"

[dependencies]
pcap-parser = { version = "0.14.0", features = ["data"] }
pdu = "1.1"

[dev-dependencies]
netgauze-bmp-service = { version = "0.1.0", path = "../bmp-service" }
serde_json = { workspace = true }
bytes = { workspace = true }
tokio-util = { workspace = true, features = ["codec"] }
