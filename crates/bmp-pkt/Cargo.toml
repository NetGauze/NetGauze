[package]
name = "netgauze-bmp-pkt"
version = "0.2.0"
edition = "2021"
authors = ["Ahmed Elhassany <a.hassany@gmail.com>"]
license = "Apache-2.0"
readme = "README.md"
repository = "https://github.com/NetGauze/NetGauze"
homepage = "https://github.com/NetGauze/NetGauze"
description = """
BMP Protocol representation and serde.
"""
keywords = ["bmp", "bgp", "parser", "protocol"]
categories = ["network-programming", "parsing"]

[dependencies]
netgauze-iana = { version = "0.2.0", path = "../iana" }
netgauze-bgp-pkt = { version = "0.2.0", path = "../bgp-pkt" }
netgauze-locate = { version = "0.2.0", path = "../locate", optional = true }
netgauze-parse-utils = { version = "0.2.0", path = "../parse-utils", optional = true }
netgauze-serde-macros = { version = "0.2.0", path = "../serde-macros", optional = true }
strum = { workspace = true }
strum_macros = { workspace = true }
chrono = { workspace = true }
ipnet = { workspace = true, features = ["serde"] }
nom = { workspace = true, optional = true }
byteorder = { workspace = true, optional = true }
criterion = { workspace = true, optional = true } # Dev dep for bench
serde = { workspace = true, features = ["derive"] }
arbitrary = { workspace = true, optional = true }
arbitrary_ext = { workspace = true, optional = true }

[features]
default = ["serde"]
serde = ["nom", "byteorder", "netgauze-locate", "netgauze-parse-utils", "netgauze-serde-macros"]
bench = ["criterion"]
fuzz = ["arbitrary", "arbitrary_ext"]

[dev-dependencies]
netgauze-parse-utils = { version = "0.2.0", path = "../parse-utils", features = ["test-helpers"] }
chrono = { workspace = true, default-features = false, features = ["std", "serde", "clock"] }
serde_json = { workspace = true }
