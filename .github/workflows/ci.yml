name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule: [cron: "40 1 * * *"]
  workflow_call:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

env:
  RUSTFLAGS: -Dwarnings

jobs:
  fmt:
    runs-on: ubuntu-latest
    name: stable / fmt
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
      - name: Install nightly
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: cargo fmt --check
        run: cargo fmt --check

  build:
    runs-on: ubuntu-latest
    name: stable / build
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
      - name: Install curl-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev
      - name: Install stable
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: cargo build
        run: cargo build --locked

  test:
    name: Rust ${{matrix.rust}}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # TODO: enable msrv checks once NetGauze is more stable
        rust: [nightly, beta, stable]
    timeout-minutes: 45
    env:
      TRYBUILD: overwrite
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{matrix.rust}}
      - name: Install curl-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev
      - name: Enable type layout randomization
        run: echo RUSTFLAGS=${RUSTFLAGS}\ -Zrandomize-layout >> $GITHUB_ENV
        if: matrix.rust == 'nightly'
      - uses: Swatinem/rust-cache@v2
      - name: run tests
        run: cargo test --locked --features iana-upstream-build

  macos:
    name: macOS
    runs-on: macos-latest
    timeout-minutes: 45
    env:
      TRYBUILD: overwrite
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@nightly
      - name: Enable type layout randomization
        run: echo RUSTFLAGS=${RUSTFLAGS}\ -Zrandomize-layout >> $GITHUB_ENV
      - uses: Swatinem/rust-cache@v2
      - name: run tests
        run: C_INCLUDE_PATH=$C_INCLUDE_PATH:$HOMEBREW_PREFIX/include cargo test --locked

  # TODO: Windows build is not working
  # windows:
  #   name: Windows
  #   runs-on: windows-latest
  #   timeout-minutes: 45
  #   env:
  #     TRYBUILD: overwrite
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: dtolnay/rust-toolchain@nightly
  #     - name: Install pcre2
  #       run: vcpkg install pcre2
  #     - name: Install dlfcn-win32
  #       run:  vcpkg install dlfcn-win32
  #     - name: Enable type layout randomization
  #       run: echo RUSTFLAGS=${RUSTFLAGS}\ -Zrandomize-layout >> $GITHUB_ENV
  #     - name: run tests
  #       run: cargo test

  # TODO: enable min version checks once NetGauze is more stable
  #  minimal:
  #    name: Minimal versions
  #    runs-on: ubuntu-latest
  #    timeout-minutes: 45
  #    env:
  #      TRYBUILD: overwrite
  #    steps:
  #      - uses: actions/checkout@v6
  #      - uses: dtolnay/rust-toolchain@nightly
  #      - run: cargo generate-lockfile -Z minimal-versions
  #      - run: cargo check --locked

  doc:
    name: Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      RUSTDOCFLAGS: -Dwarnings --cfg docsrs
      TRYBUILD: overwrite
      NETGAUZE_CUSTOM_XML_PATHS: ""
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@nightly
      - uses: dtolnay/install@cargo-docs-rs
      - name: Install curl-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev
      - uses: Swatinem/rust-cache@v2
      - name: cargo doc
        run: cargo doc --locked --no-deps --all-features

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      NETGAUZE_CUSTOM_XML_PATHS: ""
    steps:
      - name: Install curl-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@clippy
        with:
          toolchain: beta
      - uses: Swatinem/rust-cache@v2
      - name: cargo clippy
        run: cargo clippy --locked --tests --all-features

  coverage:
    # use llvm-cov to build and collect coverage and outputs in a format that is compatible with
    # codecov.io
    runs-on: ubuntu-latest
    name: ubuntu / stable / coverage
    env:
      TRYBUILD: overwrite
      NETGAUZE_CUSTOM_XML_PATHS: ""
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
      - name: Install stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - name: Install curl-dev
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev
      - uses: Swatinem/rust-cache@v2
      - name: cargo install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - name: cargo generate-lockfile
        if: hashFiles('Cargo.lock') == ''
        run: cargo generate-lockfile
      - name: cargo llvm-cov
        run: cargo llvm-cov --locked --all-features --lcov --output-path lcov.info
      - name: Upload to codecov.io
        uses: codecov/codecov-action@v6
        with:
          fail_ci_if_error: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # Define version and release information for use across jobs
  #
  # The package versioning follows the Fedora Packaging Guidelines, with a custom
  # scheme to support development builds from the main branch.
  # ref: https://docs.fedoraproject.org/en-US/packaging-guidelines/Versioning/
  #
  # RPM format: <name>-<version>-<release>.<arch>.rpm
  # DEB format: <name>_<upstream_version>-<debian_revision>_<arch>.deb
  #
  # Versioning Scheme:
  #
  # 1. Tagged Releases (e.g., refs/tags/v0.7.0):
  #    - Version: The git tag without the 'v' prefix (e.g., 0.7.0).
  #    - RPM release: '1'.
  #    - DEB revision: '1'.
  #    - Example: netgauze-collector-0.7.0-1.el8_10.x86_64.rpm
  #    - Example: netgauze-collector_0.7.0-1_amd64.deb
  #
  # 2. Development Builds (non-tagged commits on main):
  #    - These are for testing the latest changes on the main branch.
  #    - Version: Based on the latest git tag (e.g., 0.7.0).
  #    - RPM release: A preview identifier in the format
  #      '0.<commits_since_tag>^<date>git<short_sha>'.
  #    - DEB revision: A preview identifier in the format
  #      '0.<commits_since_tag>~<date>git<short_sha>'.
  #      Debian treats '~' as sorting before the final release, so development
  #      packages are ordered lower than the eventual tagged package.
  #    - Example: netgauze-collector-0.7.0-0.1^20250822gita3c6d71b.el8_10.x86_64.rpm
  #    - Example: netgauze-collector_0.7.0-0.1~20250822gita3c6d71b_amd64.deb
  #
  # Output mapping from this job:
  # - version/release are used by RPM packaging.
  # - deb_version/deb_release are used by DEB packaging.
  version:
    runs-on: ubuntu-latest
    name: Calculate version information
    outputs:
      version: ${{ steps.version_and_release.outputs.VERSION }}
      release: ${{ steps.version_and_release.outputs.RELEASE }}
      deb_version: ${{ steps.version_and_release.outputs.DEB_VERSION }}
      deb_release: ${{ steps.version_and_release.outputs.DEB_RELEASE }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # needs the full git history to get tag information
      - name: Define version and release
        id: version_and_release
        run: |
          TAG=$(git describe --tags --abbrev=0)
          VERSION=${TAG#v}
          DEB_VERSION=${VERSION}
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            RELEASE="1"
            DEB_RELEASE="1"
          else
            COMMIT_COUNT=$(git describe --tags | cut -d'-' -f2)
            RELEASE="0.${COMMIT_COUNT}^$(date +%Y%m%d)git$(git rev-parse --short=8 HEAD)"
            DEB_RELEASE="0.${COMMIT_COUNT}~$(date +%Y%m%d)git$(git rev-parse --short=8 HEAD)"
          fi
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          echo "RELEASE=${RELEASE}" >> $GITHUB_OUTPUT
          echo "DEB_VERSION=${DEB_VERSION}" >> $GITHUB_OUTPUT
          echo "DEB_RELEASE=${DEB_RELEASE}" >> $GITHUB_OUTPUT

  # Build RPM packages for different distributions
  build-collector-rpm:
    runs-on: ubuntu-latest
    needs: version
    strategy:
      matrix: &rpm-matrix
        include:
          - name: "EL 8.10"
            image: "rockylinux/rockylinux:8.10"
            el_tag: "el8_10"
            arch: "x86_64"
    name: Build RPM for ${{ matrix.name }}
    container:
      image: ${{ matrix.image }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # needs the full git history to get tag information
      - name: Install build dependencies
        run: |
          dnf install -y \
            gcc \
            gcc-c++ \
            pcre2-devel \
            perl-core \
            cmake \
            git \
            libcurl-devel \
      - name: Install stable
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-generate-rpm
        run: cargo install cargo-generate-rpm
      - name: Build netgauze-collector
        run: cargo build --locked --release -p netgauze-collector
      - name: Strip netgauze-collector binary
        run: strip target/release/netgauze-collector
      - name: Build RPM package
        run: |
          cargo generate-rpm -p crates/collector -s "version = \"${{ needs.version.outputs.version }}\"" -s "release = \"${{ needs.version.outputs.release }}.${{ matrix.el_tag }}\""
      - name: Get RPM filename
        id: rpm_filename
        run: |
          RPM_PATH=$(find target/generate-rpm -name "*.rpm")
          echo "name=$(basename "$RPM_PATH")" >> $GITHUB_OUTPUT
      - name: Upload RPM artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.rpm_filename.outputs.name }}
          path: target/generate-rpm/*.rpm
          retention-days: 30

  build-collector-deb:
    runs-on: ubuntu-latest
    needs: version
    strategy:
      matrix: &deb-matrix
        include:
          - name: "Debian 13"
            image: "debian:13"
            arch: "amd64"
    name: Build DEB for ${{ matrix.name }}
    container:
      image: ${{ matrix.image }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # needs the full git history to get tag information
      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y \
            build-essential \
            pkg-config \
            cmake \
            git \
            curl \
            libpcre2-dev \
            libcurl4-openssl-dev \
            libssl-dev
      - name: Install stable
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-deb
        run: cargo install cargo-deb
      - name: Build netgauze-collector
        run: cargo build --locked --release -p netgauze-collector
      - name: Build DEB package
        run: |
          cargo deb -p netgauze-collector --no-build --deb-version "${{ needs.version.outputs.deb_version }}-${{ needs.version.outputs.deb_release }}"
      - name: Get DEB filename
        id: deb_filename
        run: |
          DEB_PATHS=$(find target/debian -type f -name "netgauze-collector*.deb" | sort)
          DEB_COUNT=$(printf '%s\n' "$DEB_PATHS" | sed '/^$/d' | wc -l | tr -d ' ')
          if [ "$DEB_COUNT" -ne 1 ]; then
            echo "Expected exactly one DEB package, found ${DEB_COUNT}"
            printf '%s\n' "$DEB_PATHS"
            exit 1
          fi
          DEB_PATH=$(printf '%s\n' "$DEB_PATHS" | sed -n '1p')
          echo "path=${DEB_PATH}" >> "$GITHUB_OUTPUT"
          echo "name=$(basename "$DEB_PATH")" >> "$GITHUB_OUTPUT"
      - name: Upload DEB artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.deb_filename.outputs.name }}
          path: ${{ steps.deb_filename.outputs.path }}
          retention-days: 30

  test-collector-rpm:
    runs-on: ubuntu-latest
    needs: build-collector-rpm
    strategy:
      matrix: *rpm-matrix
    name: Test RPM on ${{ matrix.name }}
    container:
      image: ${{ matrix.image }}
    steps:
      - uses: actions/checkout@v6
      - name: Download RPM artifact
        uses: actions/download-artifact@v8
        with:
          path: rpm-artifact
          pattern: netgauze-collector-*${{ matrix.el_tag }}*.rpm
      - name: Install RPM
        run: |
          dnf install -y findutils
          RPM_FILE=$(find rpm-artifact -type f -name "*.rpm")
          echo "Installing RPM file: ${RPM_FILE}"
          dnf install -y "${RPM_FILE}"
      - name: Run netgauze-collector (it should start and then exit without errors)
        run: netgauze-collector crates/collector/config_ci_rpm_test.yaml

  test-collector-deb:
    runs-on: ubuntu-latest
    needs: build-collector-deb
    strategy:
      matrix: *deb-matrix
    name: Test DEB on ${{ matrix.name }}
    container:
      image: ${{ matrix.image }}
    steps:
      - uses: actions/checkout@v6
      - name: Download DEB artifact
        uses: actions/download-artifact@v8
        with:
          path: deb-artifact
          pattern: netgauze-collector*.deb
      - name: Install DEB
        run: |
          apt-get update
          if [ ! -d deb-artifact ]; then
            echo "DEB artifact directory was not created. Download step likely matched no artifacts."
            exit 1
          fi
          DEB_PATHS=$(find deb-artifact -type f -name "netgauze-collector*.deb" | sort)
          DEB_COUNT=$(printf '%s\n' "$DEB_PATHS" | sed '/^$/d' | wc -l | tr -d ' ')
          if [ "$DEB_COUNT" -ne 1 ]; then
            echo "Expected exactly one downloaded DEB package, found ${DEB_COUNT}"
            ls -la deb-artifact || true
            printf '%s\n' "$DEB_PATHS"
            exit 1
          fi
          DEB_FILE=$(printf '%s\n' "$DEB_PATHS" | sed -n '1p')
          echo "Installing DEB file: ${DEB_FILE}"
          dpkg -i "$DEB_FILE" || apt-get install -f -y
      - name: Run netgauze-collector (it should start and then exit without errors)
        run: netgauze-collector crates/collector/config_ci_rpm_test.yaml

  # Build pcap-decoder binary
  #
  # More about targets: https://doc.rust-lang.org/cargo/appendix/glossary.html#target
  # Available GitHub Actions runner images: https://github.com/actions/runner-images
  build-pcap-decoder-bin:
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container }}
    needs: version
    name: Build pcap-decoder binary (${{ matrix.dist_and_arch }})
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary_name: netgauze-pcap-decoder
            strip_command: true
            dist_and_arch: x86_64-ubuntu_latest # used just for naming the binary
          - os: ubuntu-latest
            container: rockylinux/rockylinux:8.10
            target: x86_64-unknown-linux-gnu
            binary_name: netgauze-pcap-decoder
            strip_command: true
            install_deps: |
              dnf install -y gcc gcc-c++ pcre2-devel perl-core cmake git curl
              dnf install -y https://github.com/PowerShell/PowerShell/releases/download/v7.5.2/powershell-7.5.2-1.rh.x86_64.rpm
            dist_and_arch: x86_64-el8_10 # used just for naming the binary
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary_name: netgauze-pcap-decoder.exe
            strip_command: false # strip command not available on Windows
            dist_and_arch: x86_64-windows-latest.exe # used just for naming the binary
          - os: macos-latest
            target: x86_64-apple-darwin
            binary_name: netgauze-pcap-decoder
            strip_command: true
            dist_and_arch: x86_64-apple-latest # used just for naming the binary
          - os: macos-latest
            target: aarch64-apple-darwin
            binary_name: netgauze-pcap-decoder
            strip_command: true
            dist_and_arch: aarch64-apple-latest # used just for naming the binary
    steps:
      - uses: actions/checkout@v6
      - name: Install build dependencies
        if: matrix.install_deps
        run: ${{ matrix.install_deps }}
      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
      - name: Build pcap-decoder
        run: cargo build --locked --release --target ${{ matrix.target }} -p netgauze-pcap-decoder
      - name: Strip binary (Unix only)
        if: matrix.strip_command == 'true'
        run: strip target/${{ matrix.target }}/release/${{ matrix.binary_name }}
      - name: Create complete binary file name
        id: binary_filename
        shell: pwsh # for Windows compatibility, available on all platforms
        run: |
          $name = "netgauze-pcap-decoder-${{ needs.version.outputs.version }}-${{ needs.version.outputs.release }}-${{ matrix.dist_and_arch }}"
          "name=$name" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
      - name: Rename binary
        shell: pwsh # for Windows compatibility, available on all platforms
        run: Move-Item -Path "target/${{ matrix.target }}/release/${{ matrix.binary_name }}" -Destination "target/${{ matrix.target }}/release/${{ steps.binary_filename.outputs.name }}"
      - name: Upload pcap-decoder artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.binary_filename.outputs.name }}
          path: target/${{ matrix.target }}/release/${{ steps.binary_filename.outputs.name }}
          retention-days: 30
